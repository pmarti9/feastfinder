const Users = require("../models/users");
const passport = require("../config/passport");
const { db } = mongojs() 

//establish route for models
module.exports = {
    create(req, res) {
        Users
    }
}